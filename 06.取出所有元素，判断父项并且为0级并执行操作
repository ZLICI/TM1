取维度中所有元素
v0level=Dimsiz('Account');

i=1;
    while(i<=v0level);
    vDim=Dimnm('Account',i);
i=i+1;
判断父项是否是vAccount & 是否为0level
if(ELISANC('Account', vAccount, vDim)=1&Ellev('Account',vDim)=0);

  #做出操作

endif;

end;
